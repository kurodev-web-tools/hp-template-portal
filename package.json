{
  "name": "hp-portal",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "pages:dev": "npx wrangler pages dev public --live-reload",
    "build": "node scripts/update-count.js",
    "pages:deploy": "npm run build && npx wrangler pages deploy public"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.17.0",
  "devDependencies": {
    "puppeteer": "^24.37.1",
    "serve": "^14.2.5"
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "nodemailer": "^8.0.1",
    "stripe": "^20.3.1"
  }
}